# Base URL of the backend API (no trailing slash, no /api suffix).
# Local development leaves this unset and falls back to http://localhost:8000.
# In production (Render), set this to your deployed backend URL, e.g.:
# VITE_API_URL=https://intelli-credit-backend.onrender.com
VITE_API_URL=
